{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run only the marked tests via terminal",
      "type": "node-terminal",
      "command": "npm run test-only",
      "request": "launch"
    }
  ]
}